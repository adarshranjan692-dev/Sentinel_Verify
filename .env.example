# Copy to .env only when you need custom deployment settings.
# Docker Compose supplies safe container defaults for the executable paths.
SENTINEL_SECRET_KEY=replace-with-a-long-random-secret
# Optional overrides:
# TESSERACT_CMD=/usr/bin/tesseract
# POPPLER_PATH=/usr/bin
